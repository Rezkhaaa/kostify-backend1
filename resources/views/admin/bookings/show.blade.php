@extends('layouts.admin')
@section('title','Detail Booking')
@section('content')<div class="bg-white rounded-2xl p-6 border shadow-sm"><h2 class="text-xl font-bold">{{ $booking->booking_code }}</h2><p class="mt-2">Penghuni: <b>{{ $booking->user->name ?? '-' }}</b></p><p>Kamar: <b>{{ $booking->unit->name ?? '-' }}</b></p><p>Periode: {{ $booking->start_date }} sampai {{ $booking->end_date }}</p><p>Status: <b>{{ $booking->status }}</b></p>@if($booking->status==='pending')<div class="flex gap-3 mt-5"><form method="POST" action="{{ route('admin.bookings.approve',$booking) }}">@csrf<button class="bg-green-600 text-white px-4 py-2 rounded-xl">Approve</button></form><form method="POST" action="{{ route('admin.bookings.reject',$booking) }}">@csrf<input name="reason" placeholder="Alasan penolakan" class="border rounded-xl p-2"><button class="bg-red-600 text-white px-4 py-2 rounded-xl">Reject</button></form></div>@endif</div>@endsection